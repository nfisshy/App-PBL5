import 'package:flutter/material.dart';

/// Tab giữa — để trống theo yêu cầu.
class MiddlePlaceholderTab extends StatelessWidget {
  const MiddlePlaceholderTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text(''));
  }
}
